import { Diagram } from '../types';
export declare const extension = ".uml";
export declare type Options = {
    disableMarkup?: boolean;
    disableNotes?: boolean;
};
export declare function format(diagram: Diagram, _source: string, options?: Options): string;

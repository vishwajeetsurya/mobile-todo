"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.displayName = void 0;
const DisplayNames = {
    'http:HTTP server requests': 'Web server',
};
function displayName(fqid) {
    if (DisplayNames[fqid])
        return DisplayNames[fqid];
    const tokens = fqid.split(':');
    tokens.shift();
    let name = tokens.join(':');
    if (name.includes('/'))
        name = name.split('/').pop();
    return name;
}
exports.displayName = displayName;

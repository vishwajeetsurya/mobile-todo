import { CodeObject } from './types';
export default class Priority {
    patterns: string[];
    priorityByCodeObjectGroupId: Map<string, number>;
    sequenceByCodeObjectGroupId: Map<string, number>;
    priorityByCodeObjectId: Map<string, number>;
    basePriority: number;
    enrollPattern(pattern: string): void;
    expandPattern(pattern: string, codeObjectIds: string[]): void;
    setPriority(codeObjectId: string, priority: number): void;
    priorityOf(codeObject: CodeObject): number;
    protected groupPriority(codeObject: CodeObject): {
        groupId: string;
        basePriority: number;
    };
}

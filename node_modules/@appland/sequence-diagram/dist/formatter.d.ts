import { Diagram, FormatType } from './types';
export default function format(formatType: FormatType, diagram: Diagram, source: string, options?: Record<string, boolean | string>): {
    extension: string;
    diagram: string;
};

import Command from '../command';
import { ClientRequest } from '../navie';
import VectorTermsService from '../services/vector-terms-service';
export default class VectorTermsCommand implements Command {
    private readonly vectorTermsService;
    constructor(vectorTermsService: VectorTermsService);
    execute(clientRequest: ClientRequest): AsyncIterable<string>;
}

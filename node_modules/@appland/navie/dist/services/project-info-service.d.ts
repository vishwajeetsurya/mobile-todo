import { ProjectInfo, ProjectInfoProvider } from '../project-info';
import InteractionHistory from '../interaction-history';
export default class ProjectInfoService {
    interactionHistory: InteractionHistory;
    projectInfoProvider: ProjectInfoProvider;
    constructor(interactionHistory: InteractionHistory, projectInfoProvider: ProjectInfoProvider);
    lookupProjectInfo(): Promise<ProjectInfo[]>;
    promptProjectInfo(isArchitecture: boolean, projectInfo: ProjectInfo[]): void;
}

import { ContextV2 } from '../context';
import InteractionHistory from '../interaction-history';
import { HelpResponse } from '../help';
export default class ApplyContextService {
    readonly interactionHistory: InteractionHistory;
    constructor(interactionHistory: InteractionHistory);
    applyContext(context: ContextV2.ContextResponse | undefined, help: HelpResponse, characterLimit: number, maxContentLength?: number): void;
    addSystemPrompts(context: ContextV2.ContextResponse, help: HelpResponse): void;
}

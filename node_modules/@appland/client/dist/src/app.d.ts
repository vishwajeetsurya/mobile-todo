import Mapset from './mapset';
import FindingStatusListItem from './findingStatusListItem';
import { RetryOptions } from './retryOptions';
export default class {
    fqname: string;
    constructor(fqname: string);
    mapset(mapsetId: number): Mapset;
    listFindingStatus(): Promise<FindingStatusListItem[]>;
    exists(retryOptions?: RetryOptions): Promise<boolean>;
}

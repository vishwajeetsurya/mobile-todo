/// <reference types="node" />
import { ClientRequest, RequestOptions, IncomingMessage } from 'http';
import { ServiceEndpoint } from './configuration';
export declare type Request = {
    requestFunction: (url: string | URL, options: RequestOptions, callback?: (response: IncomingMessage) => void) => ClientRequest;
    url: URL;
    headers: Record<string, string>;
};
export interface BuildRequestOptions {
    readonly requireApiKey?: boolean;
    readonly service?: ServiceEndpoint;
}
export default function buildRequest(requestPath: string, options?: BuildRequestOptions): Request;

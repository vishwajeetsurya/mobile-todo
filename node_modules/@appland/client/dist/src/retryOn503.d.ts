/// <reference types="node" />
import { IncomingMessage } from 'http';
import { RetryHandler } from '.';
export default function retry(retryHandler: RetryHandler): (response: IncomingMessage) => Promise<IncomingMessage>;

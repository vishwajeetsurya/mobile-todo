"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
const errors_1 = require("../errors");
const commandRunner_1 = require("./commandRunner");
class AgentInstaller {
    constructor(name, path) {
        this.name = name;
        /** whether the installer is noop, ie. no installation necessary */
        this.isNoop = false;
        this.path = (0, path_1.resolve)(path);
    }
    async checkCurrentConfig(ui) {
        const cmd = await this.checkConfigCommand(ui);
        if (!cmd) {
            return;
        }
        try {
            await (0, commandRunner_1.run)(cmd);
        }
        catch (err) {
            throw new errors_1.UserConfigError(err);
        }
    }
}
exports.default = AgentInstaller;
//# sourceMappingURL=agentInstaller.js.map
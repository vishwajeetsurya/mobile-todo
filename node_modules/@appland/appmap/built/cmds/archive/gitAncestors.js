"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const executeCommand_1 = require("../../lib/executeCommand");
async function gitAncestors(revision) {
    const command = `git rev-list ${revision}`;
    try {
        return (await (0, executeCommand_1.executeCommand)(command)).trim().split('\n').filter(Boolean);
    }
    catch (_a) {
        console.warn(`No ancestors found for ${revision}. Will match exact revision only.`);
        return [revision];
    }
}
exports.default = gitAncestors;
//# sourceMappingURL=gitAncestors.js.map
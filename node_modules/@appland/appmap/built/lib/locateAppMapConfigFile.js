"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.locateAppMapConfigFile = void 0;
const path_1 = require("path");
const utils_1 = require("../utils");
async function locateAppMapConfigFile(appmapDir) {
    let dir = appmapDir;
    for (;;) {
        const appmapFile = (0, path_1.join)(dir, 'appmap.yml');
        if (await (0, utils_1.exists)(appmapFile))
            return appmapFile;
        const parentDir = (0, path_1.resolve)(dir, '..');
        if (parentDir === '/' || parentDir === dir)
            break;
        dir = parentDir;
    }
}
exports.locateAppMapConfigFile = locateAppMapConfigFile;
//# sourceMappingURL=locateAppMapConfigFile.js.map
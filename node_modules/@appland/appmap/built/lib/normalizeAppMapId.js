"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const console_1 = require("console");
function normalizeAppMapId(id) {
    let normalizedId = id;
    if (normalizedId.startsWith('./'))
        normalizedId = normalizedId.slice('./'.length);
    if (normalizedId.endsWith('.appmap.json')) {
        (0, console_1.warn)(`AppMap id ${id} should not include the file extension`);
        normalizedId = normalizedId.slice(0, '.appmap.json'.length * -1);
    }
    return normalizedId;
}
exports.default = normalizeAppMapId;
//# sourceMappingURL=normalizeAppMapId.js.map